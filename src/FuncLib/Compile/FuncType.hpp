#pragma once
#include <string>

namespace FuncLib::Compile
{
	using ::std::string;
	
	// 包含包名、函数名、返回值类型、参数类型
	class FuncType
	{
	private:
		/* data */
	public:
		FuncType(/* args */);

		void PackageName(string packageName)
		{

		}
	};
}
