#pragma once
#include <vector>
#include <string>

namespace RemoteInvokeLib
{
    using ::std::string;
    using ::std::vector;

    enum class PredefinePackage
    {
        // TODO Generate
        Basic,
    };

    vector<string> ToVec(PredefinePackage package)
    {
        // TODO Generate
        return {};
    }
}
